Registro de 4 tiendas-----------------------------------


SET @idDistrito = FLOOR(RAND() * 43) + 1;

INSERT INTO Urbanizacion (nombreUrbanizacion, idDistrito)
VALUES ( 'Av. Arnaldo Márquez 1324', @idDistrito);

SET @idUrbanizacion = LAST_INSERT_ID();

INSERT INTO Direccion (nombreDireccion, idUrbanizacion)
VALUES ('Centro Comercial Santa Maria', @idUrbanizacion);

SET @idDireccion = LAST_INSERT_ID();

INSERT INTO Tienda (nombreTienda, idDireccion)
VALUES ('Máximo Store', @idDireccion);



SET @idDistrito = FLOOR(RAND() * 43) + 1;

INSERT INTO Urbanizacion (nombreUrbanizacion, idDistrito)
VALUES ( 'Jirón de la Unión', @idDistrito);

SET @idUrbanizacion = LAST_INSERT_ID();

INSERT INTO Direccion (nombreDireccion, idUrbanizacion)
VALUES ('Jirón de la Unión 466', @idUrbanizacion);

SET @idDireccion = LAST_INSERT_ID();

INSERT INTO Tienda (nombreTienda, idDireccion)
VALUES ('Pionier', @idDireccion);



SET @idDistrito = FLOOR(RAND() * 43) + 1;

INSERT INTO Urbanizacion (nombreUrbanizacion, idDistrito)
VALUES ( 'Av. Alfredo Mendiola', @idDistrito);

SET @idUrbanizacion = LAST_INSERT_ID();

INSERT INTO Direccion (nombreDireccion, idUrbanizacion)
VALUES ('Av. Alfredo Mendiola N° 1400', @idUrbanizacion);

SET @idDireccion = LAST_INSERT_ID();

INSERT INTO Tienda (nombreTienda, idDireccion)
VALUES ('H&M', @idDireccion);



SET @idDistrito = FLOOR(RAND() * 43) + 1;

INSERT INTO Urbanizacion (nombreUrbanizacion, idDistrito)
VALUES ( 'Avenida Javier Prado Este', @idDistrito);

SET @idUrbanizacion = LAST_INSERT_ID();

INSERT INTO Direccion (nombreDireccion, idUrbanizacion)
VALUES ('Avenida Javier Prado Este 4200', @idUrbanizacion);

SET @idDireccion = LAST_INSERT_ID();

INSERT INTO Tienda (nombreTienda, idDireccion)
VALUES ('Zara', @idDireccion);